import { WebpackConfig } from 'src/typings/webpack';
import { OSS_BASE_URL } from '../../constants';

export const externals = {};

export const injectAssetsOptions: WebpackConfig['injectAssets'] = {};
